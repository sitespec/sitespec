# @sitespec/core

Deterministic parser, resolver, validator, diagnostics, and inspection API for SiteSpec projects.

Most website authors should use `@sitespec/cli` instead of importing this package directly.
