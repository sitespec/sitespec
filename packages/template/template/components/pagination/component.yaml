specVersion: "0.4"
component:
  id: pagination
  role: utility

description: Render the standard SiteSpec v0.4 pagination core type as accessible static navigation.

variants:
  - default

themes:
  - default
  - muted

props:
  $ref: urn:site-spec:0.4:type:pagination

runtime:
  javascript: false

rules:
  allowedArchetypes:
    - listing
    - blank
