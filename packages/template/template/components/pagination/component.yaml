specVersion: "0.7"
component:
  id: pagination
  role: utility

description: Render the standard SiteSpec v0.7 pagination core type as accessible static navigation.

variants:
  - default

themes:
  - default
  - muted

props:
  $ref: urn:site-spec:0.7:type:pagination

runtime:
  javascript: false

rules:
  allowedArchetypes:
    - listing
    - blank
