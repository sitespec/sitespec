---
interface Props { variant?: "default" }
const { variant = "default" } = Astro.props;
---
<div class="container" data-ui="container" data-variant={variant}>
  <slot />
</div>

<style>
  .container {
    width: 100%;
    max-width: var(--size-content);
    margin-inline: auto;
  }
</style>
