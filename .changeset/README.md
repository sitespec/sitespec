# Changesets

Every user-visible change to the SiteSpec engine should include a changeset.

Run:

```bash
npm run changeset
```

All five public packages are in one fixed release group, so SiteSpec v0.1 uses one synchronized release train.
