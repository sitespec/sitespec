# Changesets

Every user-visible change to the SiteSpec engine should include a changeset.

Run:

```bash
npm run changeset
```

All five public packages are in one fixed release group, so SiteSpec uses one synchronized release train.
