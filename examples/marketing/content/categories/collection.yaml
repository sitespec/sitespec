specVersion: "0.5"
collection:
  id: categories
entry:
  schema:
    type: object
    additionalProperties: false
    required: [name, description]
    properties:
      name: { type: string }
      description: { type: string }
