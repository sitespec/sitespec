specVersion: "0.5"
component:
  id: pagination
  role: utility

description: Render the standard SiteSpec pagination core type as accessible static navigation.

variants:
  - default

themes:
  - default
  - muted

props:
  $ref: urn:site-spec:0.5:type:pagination

runtime:
  javascript: false

rules:
  allowedArchetypes:
    - listing
    - blank
