specVersion: "0.3"
component:
  id: pagination
  role: utility

description: Render the standard SiteSpec v0.3 pagination core type as accessible static navigation.

variants:
  - default

themes:
  - default
  - muted

props:
  $ref: urn:site-spec:0.3:type:pagination

runtime:
  javascript: false

rules:
  allowedArchetypes:
    - listing
    - blank
