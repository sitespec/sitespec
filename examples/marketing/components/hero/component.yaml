specVersion: "0.1"
component:
  id: hero
  role: intro

description: Primary introduction section.

variants:
  - default
  - centered
  - split

themes:
  - default
  - muted
  - inverted

props:
  type: object
  additionalProperties: false
  required:
    - title
  properties:
    eyebrow:
      type: string
      maxLength: 40
    title:
      type: string
      maxLength: 90
    text:
      type: string
      maxLength: 240
    primaryAction:
      $ref: urn:site-spec:0.1:type:action

rules:
  maxPerPage: 1
  allowedArchetypes:
    - marketing
    - detail
  placement: first

semantics:
  pageHeading: true
